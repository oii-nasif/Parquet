// Chapter 2: Lakehouse I - Lakehouse Concepts
const QUESTIONS = [
  {"num": 1, "question": "Which storage format is used by default for tables in a Microsoft Fabric lakehouse?", "options": ["A. Parquet", "B. Delta Lake", "C. CSV", "D. JSON"], "answer": "B", "explanation": "Delta Lake (based on Parquet) is the default storage format for Fabric lakehouse tables. Delta Lake provides ACID transactions, time travel, and schema evolution on top of Parquet files."},
  {"num": 2, "question": "What is the primary difference between a lakehouse and a data warehouse in Microsoft Fabric?", "options": ["A. Lakehouse uses SQL only", "B. Warehouse stores unstructured data", "C. Lakehouse supports both SQL and Spark", "D. Warehouse is faster for all workloads"], "answer": "C", "explanation": "Lakehouse combines the capabilities of a data lake (supporting unstructured/semi-structured data and Spark processing) with the structured query capabilities of a data warehouse. Warehouses are optimized for SQL workloads using T-SQL."},
  {"num": 3, "question": "You need to store JSON files from an API in a lakehouse for later processing. Which folder structure is recommended?", "options": ["A. Store in Tables folder", "B. Store in Files folder with subfolders by date", "C. Store directly in lakehouse root", "D. Create a separate lakehouse for files"], "answer": "B", "explanation": "For raw files like JSON, store them in the Files folder with organized subfolders (e.g., by date or source). The Tables folder is specifically for managed Delta tables. This keeps raw and processed data separate."},
  {"num": 4, "question": "Which command in PySpark creates a managed Delta table in a Fabric lakehouse?", "options": ["A. spark.sql(\"CREATE TABLE\")", "B. df.write.format(\"delta\").saveAsTable()", "C. df.write.parquet()", "D. CREATE EXTERNAL TABLE"], "answer": "B", "explanation": "df.write.format(\"delta\").saveAsTable() creates a managed Delta table in a lakehouse. The table is managed by Fabric, meaning the metadata and data are handled automatically. CREATE TABLE also works but requires SQL syntax."},
  {"num": 5, "question": "You need to query CSV files stored in the Files section of a lakehouse using Spark. Which method is most efficient?", "options": ["A. Read each file individually", "B. Use wildcard path with spark.read", "C. Copy to Tables folder first", "D. Use Lakehouse explorer only"], "answer": "B", "explanation": "Using spark.read with wildcard paths (e.g., \"Files/*.csv\") is efficient for reading multiple CSV files. Spark optimizes parallel reading of the files. You can also specify schema for better performance."},
  {"num": 6, "question": "What is the purpose of the Tables folder in a Fabric lakehouse?", "options": ["A. Stores raw data files", "B. Stores managed Delta tables", "C. Contains notebook outputs", "D. Holds temporary files"], "answer": "B", "explanation": "The Tables folder contains managed Delta tables created in the lakehouse. These tables have ACID transactions, time travel, and schema evolution. The Files folder is for raw or semi-structured data files."},
  {"num": 7, "question": "You need to read a Delta table from a lakehouse in a PySpark notebook. Which command should you use?", "options": ["A. spark.read.table(\"table_name\")", "B. spark.read.format(\"delta\").load(\"table_name\")", "C. spark.sql(\"SELECT * FROM table_name\")", "D. All work"], "answer": "D", "explanation": "All three methods work for reading Delta tables in Fabric lakehouse. spark.read.table() is the simplest, spark.sql() allows complex queries, and format('delta').load() gives you more control over options."},
  {"num": 8, "question": "Which statement is true about the SQL analytics endpoint of a lakehouse?", "options": ["A. Supports all DML operations", "B. Is read-only", "C. Requires separate licensing", "D. Only works with external tables"], "answer": "B", "explanation": "The SQL analytics endpoint of a lakehouse is read-only. You can query Delta tables using T-SQL but cannot perform INSERT, UPDATE, DELETE, or DDL operations. For write operations, use Spark notebooks or warehouses."},
  {"num": 9, "question": "You need to create a Delta table with partitioning on a date column. Which PySpark command should you use?", "options": ["A. .partitionBy(\"date_col\")", "B. .write.partitionBy(\"date_col\")", "C. CREATE TABLE ... PARTITIONED BY", "D. All can be used"], "answer": "D", "explanation": "All three methods can create partitioned Delta tables. partitionBy() in DataFrameWriter is common for Spark, while PARTITIONED BY in SQL CREATE TABLE also works. Partitioning improves query performance for filters on the partition column."},
  {"num": 10, "question": "What happens when you delete a managed Delta table in a lakehouse?", "options": ["A. Data is immediately deleted", "B. Metadata is removed, data stays until VACUUM", "C. Both metadata and data are immediately deleted", "D. Data is moved to recycle bin"], "answer": "B", "explanation": "When you delete a managed Delta table, the table metadata is immediately removed, but the underlying Parquet files remain until VACUUM is run. This provides a safety net and enables time travel to recover dropped tables."},
  {"num": 11, "question": "You need to store unstructured data (images, videos) in a lakehouse. Where should you store them?", "options": ["A. Tables folder as Delta table", "B. Files folder with appropriate subfolders", "C. OneLake shortcuts only", "D. Create a separate warehouse"], "answer": "B", "explanation": "Unstructured data like images and videos should be stored in the Files folder of the lakehouse. Delta tables in the Tables folder are designed for structured, tabular data. Use organized subfolders to manage unstructured content."},
  {"num": 12, "question": "Which Delta Lake feature allows you to query data as it existed at a previous point in time?", "options": ["A. Schema evolution", "B. Time Travel", "C. ACID transactions", "D. Data skipping"], "answer": "B", "explanation": "Time Travel in Delta Lake allows you to query previous versions of data using a timestamp or version number. This is useful for auditing, data recovery, reproducibility, and 'what if' analysis."},
  {"num": 13, "question": "You need to add a new column to an existing Delta table. Which feature allows this without rewriting the entire table?", "options": ["A. Data skipping", "B. Schema evolution", "C. Z-ordering", "D. VACUUM"], "answer": "B", "explanation": "Schema evolution allows you to add, drop, or rename columns in a Delta table without rewriting existing data files. New columns are added to the schema, and queries that don't reference them continue working with the old files."},
  {"num": 14, "question": "What is the medallion architecture in data engineering?", "options": ["A. Single-layer storage", "B. Bronze (raw) → Silver (cleaned) → Gold (aggregated)", "C. Gold → Silver → Bronze flow", "D. Random data distribution"], "answer": "B", "explanation": "Medallion architecture organizes data into three layers: Bronze (raw data), Silver (cleaned and validated), and Gold (aggregated for reporting). Data flows from Bronze to Silver to Gold, with each layer adding value through transformation."},
  {"num": 15, "question": "You need to create an external table in a lakehouse that points to Parquet files in the Files folder. Which command should you use?", "options": ["A. CREATE TABLE", "B. CREATE EXTERNAL TABLE", "C. spark.read.parquet().saveAsTable()", "D. registerTable()"], "answer": "B", "explanation": "CREATE EXTERNAL TABLE creates a table definition that references existing Parquet files without moving them. The data stays in the Files folder, and changes to files are immediately reflected in queries. Managed tables (CREATE TABLE) copy data."},
  {"num": 16, "question": "Which file format provides the best compression for analytical workloads in Fabric lakehouse?", "options": ["A. CSV", "B. JSON", "C. Parquet", "D. Avro"], "answer": "C", "explanation": "Parquet provides the best compression for analytical workloads due to its columnar storage format. It's especially efficient for queries that read only a subset of columns. Delta Lake builds on Parquet, adding ACID transactions and other features."},
  {"num": 17, "question": "You need to update a specific row in a Delta table. Which operation should you use?", "options": ["A. INSERT", "B. UPDATE", "C. MERGE", "D. APPEND"], "answer": "C", "explanation": "MERGE (upsert) is the most efficient operation for updating specific rows in a Delta table. It can match existing rows based on keys and update them, or insert new rows if no match is found. This is commonly used for CDC and SCD scenarios."},
  {"num": 18, "question": "What is the purpose of the _delta_log folder in a Delta table?", "options": ["A. Stores table statistics", "B. Tracks all transactions and changes", "C. Contains cached data", "D. Holds temporary files"], "answer": "B", "explanation": "The _delta_log folder contains the transaction log that records all changes to the Delta table. It enables ACID transactions, time travel, and schema evolution. Each commit creates a new JSON file in this log."},
  {"num": 19, "question": "You need to read a specific version of a Delta table as it existed 7 days ago. Which syntax should you use?", "options": ["A. VERSION AS OF 7", "B. TIMESTAMP AS OF '2024-01-01'", "C. Both work", "D. Neither is supported"], "answer": "C", "explanation": "Delta Lake Time Travel supports both VERSION AS OF (for version number) and TIMESTAMP AS OF (for date/time). This flexibility allows you to query data as it existed at a specific point in the past."},
  {"num": 20, "question": "Which command removes old files from a Delta table to free up storage space?", "options": ["A. DELETE FROM", "B. DROP TABLE", "C. VACUUM", "D. OPTIMIZE"], "answer": "C", "explanation": "VACUUM removes old file versions that are no longer needed for time travel or recent operations. By default, files older than 7 days are removed. This reclaims storage space but doesn't compact the data."},
  {"num": 21, "question": "You need to improve query performance on a Delta table by reorganizing data layout. Which command should you use?", "options": ["A. VACUUM", "B. OPTIMIZE", "C. ZORDER BY", "D. REPARTITION"], "answer": "B", "explanation": "OPTIMIZE compacts small files into larger ones and can apply Z-ordering. This improves query performance by reducing the number of files to scan and improving data skipping. ZORDER BY is an option within OPTIMIZE."},
  {"num": 22, "question": "What is the difference between a managed table and an external table in a lakehouse?", "options": ["A. Managed tables are faster", "B. External tables don't support SQL", "C. Managed tables store data in Tables folder, external tables reference Files", "D. No difference"], "answer": "C", "explanation": "Managed tables are stored in the Tables folder with data managed by Fabric. External tables reference data in the Files folder or other locations. Managed tables support ACID transactions and time travel; external tables don't guarantee these."},
  {"num": 23, "question": "You need to create a Delta table with clustering (Z-ordering) on multiple columns. Which syntax should you use?", "options": ["A. CREATE TABLE ... CLUSTER BY", "B. OPTIMIZE ... ZORDER BY (col1, col2)", "C. .write.partitionBy('col1', 'col2')", "D. CREATE INDEX"], "answer": "B", "explanation": "Z-ordering is applied using OPTIMIZE with ZORDER BY. You can specify multiple columns: OPTIMIZE table ZORDER BY (col1, col2, col3). This collocates related data in the same files for improved data skipping."},
  {"num": 24, "question": "Which statement is true about Delta Lake's support for ACID transactions?", "options": ["A. Only supports atomic operations", "B. Supports atomic, consistent, isolated, durable operations", "C. Atomic only, no isolation", "D. No transaction support"], "answer": "B", "explanation": "Delta Lake provides full ACID transaction support: Atomicity (all-or-nothing commits), Consistency (valid data at all times), Isolation (concurrent operations don't interfere), and Durability (committed data persists)."},
  {"num": 25, "question": "You need to convert CSV files in the Files folder to a Delta table. Which approach is most efficient?", "options": ["A. Read CSV and saveAsTable", "B. Use COPY INTO", "C. Use CREATE TABLE AS SELECT", "D. All are equally efficient"], "answer": "A", "explanation": "Reading CSV files with spark.read and saving as a Delta table is the standard approach. COPY INTO and CTAS also work but may be less flexible for complex transformations. The key is using Delta format for the destination for better performance."},
  {"num": 26, "question": "What is the purpose of checkpointing in Delta Lake?", "options": ["A. Improve query performance", "B. Compact transaction log for faster recovery", "C. Enable schema evolution", "D. Create backups"], "answer": "B", "explanation": "Checkpointing compacts the transaction log (_delta_log) into larger files. This speeds up operations that need to read the log, such as listing table history or time travel to older versions. It's done automatically after 10 commits."},
  {"num": 27, "question": "You need to create a Delta table that supports concurrent reads and writes. Does Delta Lake support this?", "options": ["A. No, only single writer", "B. Yes, full ACID support with optimistic concurrency", "C. Yes, but with locking", "D. Only for append operations"], "answer": "B", "explanation": "Delta Lake supports concurrent reads and writes with full ACID guarantees. Multiple users can read and write simultaneously using optimistic concurrency control. Conflicting writes are automatically retried."},
  {"num": 28, "question": "Which operation should you use to delete all rows from a Delta table while keeping the table definition?", "options": ["A. DROP TABLE", "B. DELETE FROM", "C. TRUNCATE TABLE", "D. REMOVE ALL"], "answer": "C", "explanation": "TRUNCATE TABLE removes all rows from a Delta table while keeping the table definition. It's faster than DELETE FROM for large tables because it doesn't scan the data. DROP TABLE would remove both data and table definition."},
  {"num": 29, "question": "You need to query a Delta table that is being updated by another process. Can you read the table while it's being written to?", "options": ["A. No, table is locked during writes", "B. Yes, reads are isolated from writes", "C. Only if using snapshot isolation", "D. Must wait for write to complete"], "answer": "B", "explanation": "Delta Lake provides snapshot isolation, so readers can query the table while writes are happening. Readers see a consistent snapshot of the data as of the query start time, not affected by concurrent writes."},
  {"num": 30, "question": "What is the data skipping feature in Delta Lake?", "options": ["A. Skips duplicate rows", "B. Skips irrelevant files based on filters", "C. Skips null values", "D. Skips corrupt data"], "answer": "B", "explanation": "Data skipping in Delta Lake allows queries to skip entire files based on filter predicates. By using statistics (min/max values) stored in the transaction log, Delta can determine which files don't contain relevant data and skip reading them."},
  {"num": 31, "question": "You need to restore a Delta table to a previous version after an accidental bad update. Which command should you use?", "options": ["A. RESTORE TABLE", "B. UNDROP TABLE", "C. ROLLBACK", "D. REVERT TO VERSION"], "answer": "A", "explanation": "RESTORE TABLE ... TO VERSION AS OF allows you to restore a Delta table to a previous version. You can specify a version number or timestamp. This is particularly useful for recovering from accidental updates or deletions."},
  {"num": 32, "question": "Which file format does Delta Lake use for storing data?", "options": ["A. CSV", "B. JSON", "C. Parquet", "D. ORC"], "answer": "C", "explanation": "Delta Lake uses Parquet format for storing data files. Parquet is a columnar storage format that provides excellent compression and query performance for analytical workloads. Delta adds a transaction log on top of Parquet."},
  {"num": 33, "question": "You need to perform a upsert (update if exists, insert if new) operation on a Delta table. Which operation should you use?", "options": ["A. INSERT with ON DUPLICATE KEY UPDATE", "B. MERGE INTO", "C. UPDATE with INSERT if not exists", "D. REPLACE INTO"], "answer": "B", "explanation": "MERGE INTO is the standard operation for upserts in Delta Lake. It matches rows based on keys, updates matched rows, and inserts unmatched rows. This is commonly used for change data capture and slowly changing dimensions."},
  {"num": 34, "question": "What is the retention period for Delta Lake time travel by default?", "options": ["A. 7 days", "B. 30 days", "C. Unlimited until VACUUM", "D. 24 hours"], "answer": "C", "explanation": "Delta Lake retains all historical versions by default until VACUUM is run. After VACUUM, versions older than the retention period (default 7 days) are permanently deleted and can no longer be accessed via time travel."},
  {"num": 35, "question": "You need to create a view on a Delta table that always shows the latest data. Will the view automatically reflect changes to the table?", "options": ["A. Yes, views are automatically updated", "B. No, views must be recreated", "C. Only if using REFRESH VIEW", "D. Views don't support Delta tables"], "answer": "A", "explanation": "Views in Spark (and Delta Lake) are logical views that always reflect the current state of the underlying table. When you query a view, it reads the latest data, so no manual refresh is needed. Changes to the table are immediately visible through the view."},
  {"num": 36, "question": "Which command should you use to see the history of commits for a Delta table?", "options": ["A. DESCRIBE HISTORY", "B. SHOW HISTORY", "C. deltaTable.history()", "D. All work"], "answer": "A", "explanation": "DESCRIBE HISTORY table_name (SQL) or deltaTable.history() (Python) shows the commit history for a Delta table. This includes operation type, user, timestamp, and other metadata for each transaction."},
  {"num": 37, "question": "You need to add a generated column (like current timestamp) to a Delta table. How should you do this?", "options": ["A. Use DEFAULT constraint in CREATE TABLE", "B. Add column in generated always as", "C. Use computed column in SELECT", "D. Delta doesn't support generated columns"], "answer": "B", "explanation": "Delta Lake supports GENERATED ALWAYS AS for computed columns during writes. The column is automatically computed when rows are inserted. For time-based columns, use CURRENT_TIMESTAMP or similar functions in the generated expression."},
  {"num": 38, "question": "What is the difference between saveAsTable and save in PySpark for Delta tables?", "options": ["A. saveAsTable creates managed, save creates external", "B. saveAsTable overwrites, save appends", "C. No difference", "D. save is for Parquet only"], "answer": "A", "explanation": "saveAsTable() creates a managed Delta table (data in Tables folder). save() with path creates an external table or saves directly to a path. Both can write Delta format, but managed tables have additional features like time travel."},
  {"num": 39, "question": "You need to query a Delta table using both Spark SQL and T-SQL. Can you do this?", "options": ["A. Yes, both SQL endpoints support Delta", "B. No, must use Spark SQL only", "C. Yes, but only through warehouse", "D. T-SQL doesn't support Delta tables"], "answer": "A", "explanation": "Delta tables can be queried using both Spark SQL (in notebooks) and T-SQL (through the SQL analytics endpoint of the lakehouse or a warehouse). This provides flexibility for users with different skills and tools."},
  {"num": 40, "question": "Which operation should you use to change the data type of an existing column in a Delta table?", "options": ["A. ALTER TABLE ... ALTER COLUMN", "B. ALTER TABLE ... CHANGE COLUMN", "C. ALTER TABLE ... SET COLUMN TYPE", "D. Recreate table with new schema"], "answer": "A", "explanation": "ALTER TABLE ... ALTER COLUMN changes the data type of an existing column in Delta Lake. This is a schema evolution feature that doesn't require rewriting the entire table. The column's metadata is updated without moving data."},
  {"num": 41, "question": "You need to stream data into a Delta table in real-time. Which feature supports this?", "options": ["A. Structured Streaming", "B. Bulk copy", "C. Batch import", "D. Manual INSERT"], "answer": "A", "explanation": "Structured Streaming in Spark (including PySpark) supports writing streaming data to Delta tables. Using foreachBatch or writeStream, you can continuously append data as it arrives, with exactly-once guarantees and automatic schema handling."},
  {"num": 42, "question": "What is the purpose of GENERATE symlink in Delta Lake?", "options": ["A. Create a shortcut to another table", "B. Create a symbolic link in file system", "C. Create a view that references external data", "D. Backup the table"], "answer": "C", "explanation": "GENERATE symlink creates a view-like object that references external data sources (Parquet, JSON, CSV) without copying the data. This is similar to an external table but provides more flexibility for querying various formats as if they were Delta tables."},
  {"num": 43, "question": "You need to find large files in a Delta table that should be compacted. Which command shows file sizes?", "options": ["A. DESCRIBE DETAIL", "B. SHOW FILES", "C. deltaTable.detail()", "D. LIST FILES"], "answer": "A", "explanation": "DESCRIBE DETAIL table_name (SQL) or deltaTable.detail() (Python) shows detailed information about files including size, partitioning, and other statistics. This helps identify small files that should be compacted using OPTIMIZE."},
  {"num": 44, "question": "Which statement is true about concurrency control in Delta Lake?", "options": ["A. Uses pessimistic locking", "B. Uses optimistic concurrency with retry", "C. No concurrency support", "D. Requires manual conflict resolution"], "answer": "B", "explanation": "Delta Lake uses optimistic concurrency control. Multiple writers can operate simultaneously, and conflicts are automatically detected and retried. This provides better throughput than locking approaches for most workloads."},
  {"num": 45, "question": "You need to create a Delta table with a specific location in the Files folder. Which syntax should you use?", "options": ["A. CREATE TABLE ... LOCATION", "B. CREATE EXTERNAL TABLE ... LOCATION", "C. saveAsTable with path option", "D. Both B and C work"], "answer": "D", "explanation": "Both CREATE EXTERNAL TABLE ... LOCATION and saveAsTable() with a path can create a table at a specific location. The LOCATION clause specifies where data is stored, allowing you to organize files as needed."},
  {"num": 46, "question": "What happens when you run OPTIMIZE on a Delta table?", "options": ["A. Only compacts files", "B. Only applies Z-ordering", "C. Can both compact and Z-order", "D. Deletes old files"], "answer": "C", "explanation": "OPTIMIZE can compact small files into larger ones and optionally apply Z-ordering. Use OPTIMIZE table for compaction only, or OPTIMIZE table ZORDER BY (cols) to also collocate data for improved filtering."},
  {"num": 47, "question": "You need to change the nullability constraint of a column in a Delta table. Can you do this?", "options": ["A. No, constraints cannot be changed", "B. Yes, using ALTER TABLE ... SET NOT NULL", "C. Yes, by recreating the table", "D. Yes, using SET NULL"], "answer": "C", "explanation": "Delta Lake doesn't support ALTER COLUMN to change nullability directly. To change a column's nullability, you must recreate the table with the new schema or use a workaround like adding a new column and dropping the old one."},
  {"num": 48, "question": "Which command should you use to remove old file versions older than 30 days from a Delta table?", "options": ["A. VACUUM RETAIN 30 DAYS", "B. VACUUM", "C. DELETE HISTORY", "D. CLEANUP FILES"], "answer": "A", "explanation": "VACUUM table RETAIN 30 DAYS removes file versions older than 30 days. By default, VACUUM retains 7 days. The RETAIN parameter allows you to control how much history to keep for time travel."},
  {"num": 49, "question": "You need to read only specific columns from a large Delta table. Which approach is most efficient?", "options": ["A. Read all columns and select", "B. Specify columns in spark.read", "C. Create a view with selected columns", "D. Use column pruning"], "answer": "B", "explanation": "Specifying columns in spark.read (e.g., spark.read.table('table').select('col1', 'col2')) enables column pruning. Delta Lake and Parquet will only read the specified columns from storage, significantly improving performance for wide tables."},
  {"num": 50, "question": "What is the purpose of the change data feed (CDF) feature in Delta Lake?", "options": ["A. Track row-level changes", "B. Compress data", "C. Enable schema evolution", "D. Improve query performance"], "answer": "A", "explanation": "Change Data Feed (CDF) in Delta Lake captures row-level changes (insert, update, delete) and exposes them as a streaming source. This enables downstream systems to consume only the changes rather than the entire table, ideal for CDC and real-time scenarios."}
];
