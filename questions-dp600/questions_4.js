// Chapter 4: Warehouse I - T-SQL & Modeling
const QUESTIONS = [
  {"num": 1, "question": "Which relationship cardinality in Fabric warehouse modeling enforces referential integrity by preventing orphaned records?", "options": ["A. One-to-one", "B. One-to-many", "C. Many-to-many", "D. All enforce referential integrity"], "answer": "B", "explanation": "One-to-many relationships can enforce referential integrity through foreign key constraints. The many side must reference valid primary keys from the one side. Many-to-many requires a junction table and doesn't directly enforce integrity."},
  {"num": 2, "question": "You need to create a slowly changing dimension (SCD) Type 2 to track historical changes to customer addresses. Which T-SQL feature is most appropriate?", "options": ["A. MERGE statement", "B. UPDATE with JOIN", "C. INSERT with triggers", "D. Temporal tables"], "answer": "A", "explanation": "MERGE statement is ideal for SCD Type 2 implementation as it can handle inserts for new records, updates for existing ones with effective date tracking, and expires old records in a single operation. MERGE simplifies the complex logic required for tracking history."},
  {"num": 3, "question": "Which index type in a Fabric warehouse is most appropriate for columns frequently used in JOIN predicates?", "options": ["A. Columnstore index", "B. Nonclustered index", "C. Clustered index", "D. Full-text index"], "answer": "C", "explanation": "Clustered indexes are best for columns frequently used in JOINs and WHERE clauses because they physically order the data. In Fabric warehouses, tables typically have clustered indexes on primary keys for optimal join performance."},
  {"num": 4, "question": "You need to create a star schema with a fact table and dimension tables. Which table type should use clustered columnstore indexes?", "options": ["A. Dimension tables only", "B. Fact table only", "C. Both fact and dimension tables", "D. Neither, use clustered indexes"], "answer": "C", "explanation": "Both fact and dimension tables benefit from clustered columnstore indexes in analytics workloads. Fact tables benefit from compression and scan performance, while dimension tables also benefit from columnstore for their typically smaller size. All warehouse tables default to columnstore."},
  {"num": 5, "question": "What is the purpose of a foreign key constraint in a Fabric warehouse?", "options": ["A. Improve query performance", "B. Enforce referential integrity", "C. Enable compression", "D. Create indexes automatically"], "answer": "B", "explanation": "Foreign key constraints enforce referential integrity by ensuring that values in one table reference valid keys in another table. This prevents orphaned records and maintains data consistency across related tables."},
  {"num": 6, "question": "You need to create a computed column that combines first name and last name. Which syntax should you use?", "options": ["A. AS (FirstName + ' ' + LastName)", "B. AS (CONCAT(FirstName, ' ', LastName))", "C. AS (FirstName + LastName)", "D. Both A and B work"], "answer": "B", "explanation": "CONCAT is the standard T-SQL function for string concatenation. While + also works for strings, CONCAT is more explicit and handles NULL values differently (NULL + anything = NULL in standard SQL, but CONCAT ignores NULLs in some configurations)."},
  {"num": 7, "question": "Which T-SQL function should you use to calculate the difference between two dates in days?", "options": ["A. SUBTRACT", "B. DATEDIFF", "C. DATEPART", "D. DATEDADD"], "answer": "B", "explanation": "DATEDIFF(day, start_date, end_date) returns the difference in days between two dates. DATEDIFF is versatile and can calculate difference in various units (year, quarter, month, day, hour, minute, second)."},
  {"num": 8, "question": "You need to create a unique constraint on a column that allows NULL values. Which syntax should you use?", "options": ["A. UNIQUE NOT NULL", "B. UNIQUE (column_name)", "C. UNIQUE constraint allows NULL automatically", "D. Use PRIMARY KEY instead"], "answer": "B", "explanation": "UNIQUE constraint allows one NULL value per column by default (since NULL != NULL). Multiple NULLs are allowed because they aren't considered equal. For truly unique values excluding NULLs, add NOT NULL. UNIQUE constraint enforces uniqueness on non-NULL values."},
  {"num": 9, "question": "What is the primary benefit of using a clustered columnstore index (CCI) for analytical queries?", "options": ["A. Faster single-row lookups", "B. Better compression and scan performance", "C. Supports faster updates", "D. Lower memory usage"], "answer": "B", "explanation": "Clustered columnstore indexes provide excellent compression for analytical workloads and fast scan performance for aggregations. They're optimized for reading large ranges of rows, not for single-row lookups (where nonclustered indexes are better)."},
  {"num": 10, "question": "You need to implement a snowflake schema with a dimension table referencing another dimension. Is this pattern recommended?", "options": ["A. Yes, snowflake is standard", "B. No, use star schema instead", "c. Only if using views", "d. Only for small datasets"], "answer": "B", "explanation": "Snowflake schemas (dimensions referencing other dimensions) are generally not recommended in data warehousing. Use star schema with all dimensions connecting directly to the fact table. Snowflaking introduces complexity without significant benefits and hurts query performance."},
  {"num": 11, "question": "Which T-SQL clause should you use to filter aggregated results using HAVING?", "options": ["A. WHERE", "B. GROUP BY", "C. HAVING", "D. FILTER"], "answer": "C", "explanation": "HAVING filters aggregated results after GROUP BY. Use HAVING to filter on the results of aggregations (like COUNT, SUM, AVG), while WHERE filters raw rows before aggregation. HAVING is the only clause that can filter aggregated data."},
  {"num": 12, "question": "You need to create a view that joins three tables. Which view type should you use for a simple join with no aggregation?", "options": ["A. Standard view", "B. Indexed view", "C. Materialized view", "D. Snapshot view"], "answer": "A", "explanation": "Use a standard view (CREATE VIEW) for simple joins. Standard views don't store data; they just store the query definition. Indexed views can improve performance but have restrictions and add overhead. Materialized views are for complex aggregations."},
  {"num": 13, "question": "What is the purpose of the OPTION (RECOMPILE) hint in T-SQL queries?", "options": ["A. Force a cache miss", "B. Generate a new query plan", "C. Use parallel execution", "d. Improve parameter sniffing"], "answer": "B", "explanation": "OPTION (RECOMPILE) forces the query optimizer to generate a new execution plan for the query. This is useful when you suspect the cached plan is suboptimal due to parameter sniffing or data distribution changes."},
  {"num": 14, "question": "You need to rank customers by their total purchases. Which window function should you use?", "options": ["A. ROW_NUMBER()", "B. RANK()", "C. DENSE_RANK()", "d. NTILE()"], "answer": "C", "explanation": "DENSE_RANK() is typically best for ranking with ties - customers with the same purchase amount get the same rank, and the next rank is not skipped. ROW_NUMBER() gives unique ranks even for ties, RANK() skips ranks for ties. NTILE() divides into equal-sized buckets."},
  {"num": 15, "question": "Which T-SQL function should you use to pivot rows into columns for reporting?", "options": ["A. PIVOT", "B. UNPIVOT", "c. CROSS APPLY", "D. STRING_AGG"], "answer": "A", "explanation": "PIVOT rotates rows into columns, aggregating data in the process. For example, you can pivot monthly sales data so each month becomes a column. UNPIVOT does the opposite - rotates columns into rows. Use PIVOT with aggregate functions like SUM or MAX."},
  {"num": 16, "question": "You need to create a calculated measure in a warehouse that will be used in Power BI. Where should you create this measure?", "options": ["A. As a computed column in the warehouse", "B. As a view", "C. In Power BI semantic model", "D. All three work equally"], "answer": "C", "explanation": "Measures for Power BI should be created in the Power BI semantic model (using DAX), not in the warehouse. The warehouse stores raw data, while the semantic model defines business logic. Computed columns in warehouses are pre-calculated and don't respond to filters."},
  {"num": 17, "question": "What is the difference between INNER JOIN and LEFT JOIN in T-SQL?", "options": ["A. INNER JOIN includes all rows, LEFT JOIN includes matches only", "B. INNER JOIN returns matches only, LEFT JOIN includes all from left table", "C. Performance difference only", "D. No difference"], "answer": "B", "explanation": "INNER JOIN returns only rows where both tables have matches. LEFT JOIN returns all rows from the left table, plus matched rows from the right table (NULLs where no match). LEFT JOIN is more inclusive - use it when you want to preserve all rows from the primary table."},
  {"num": 18, "question": "You need to create a surrogate key for a slowly changing dimension. Which data type is most appropriate?", "options": ["A. INT IDENTITY", "B. BIGINT IDENTITY", "C. UNIQUEIDENTIFIER", "D. VARCHAR hash"], "answer": "A", "explanation": "INT IDENTITY is sufficient for most SCD scenarios (up to 2 billion records). BIGINT is only needed for extremely high volume. UNIQUEIDENTIFIER (GUID) is larger and slower for joins. Use INT IDENTITY for SCD Type 2 surrogate keys."},
  {"num": 19, "question": "Which T-SQL feature allows you to update a table from another table in a single statement?", "options": ["A. UPDATE FROM", "B. MERGE", "C. INSERT ... SELECT", "d. BULK INSERT"], "answer": "B", "explanation": "MERGE can synchronize data between two tables in one statement, handling updates, inserts, and deletes based on matching conditions. UPDATE FROM can update from another table but doesn't handle inserts/deletes. MERGE is ideal for SCD implementations."},
  {"num": 20, "question": "You need to find duplicate rows in a warehouse table. Which query pattern is most efficient?", "options": ["A. GROUP BY ... HAVING COUNT(*) > 1", "B. ROW_NUMBER() OVER (PARTITION BY ...)", "C. Self-join with GROUP BY", "D. EXISTS with subquery"], "answer": "A", "explanation": "GROUP BY ... HAVING COUNT(*) > 1 is the simplest and most efficient way to find duplicates. Use it on the key columns to identify which values appear multiple times. ROW_NUMBER() is useful when you need to deduplicate while preserving one row."},
  {"num": 21, "question": "What is the purpose of a covering index in T-SQL?", "options": ["A. Includes all columns needed by a query", "B. Covers multiple tables", "C. Optimizes for ORDER BY", "d. Covers all indexes"], "answer": "A", "explanation": "A covering index includes all columns referenced by a query (in SELECT, WHERE, JOIN, GROUP BY). This allows the query to be satisfied by reading the index alone without accessing the base table, improving performance through 'index-only' scans."},
  {"num": 22, "question": "You need to implement a parent-child hierarchy in a warehouse. Which design pattern is recommended?", "options": ["A. Recursive CTE with path column", "B. Adjacency list model", "c. Nested set model", "d. Materialized path"], "answer": "A", "explanation": "Recursive CTE (Common Table Expression) with a path column is the recommended approach for hierarchies in T-SQL. The path column stores the entire hierarchy path (e.g., '/1/3/7/'), making queries for ancestors/descendants efficient."},
  {"num": 23, "question": "Which T-SQL function should you use to calculate a running total of sales?", "options": ["A. SUM() OVER (ORDER BY date)", "B. SUM() OVER (ORDER BY date ROWS BETWEEN...)", "c. RUNNING_TOTAL()", "d. PARTITION BY date"], "answer": "B", "explanation": "SUM() OVER (ORDER BY date ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) creates a running total. ROWS BETWEEN... defines the window frame. This pattern is used for cumulative calculations like running totals, moving averages, etc."},
  {"num": 24, "question": "What is the difference between DELETE and TRUNCATE in a Fabric warehouse?", "options": ["A. DELETE can use WHERE, TRUNCATE cannot", "B. TRUNCATE can use WHERE, DELETE cannot", "C. DELETE is faster, TRUNCATE is slower", "D. No functional difference"], "answer": "A", "explanation": "DELETE can use a WHERE clause to remove specific rows, while TRUNCATE removes all rows. TRUNCATE is faster and minimally logged but cannot be filtered. DELETE is fully logged and can be rolled back (within transaction limits)."},
  {"num": 25, "question": "You need to create a view that pre-aggregates data for common queries. What is the primary concern?", "options": ["A. Views are always up-to-date", "B. Views can be out of date if base data changes", "C. Views don't support joins", "D. Views cannot be indexed"], "answer": "B", "explanation": "Views are always up-to-date when queried (they don't store data). The concern is that complex views with aggregations can be slow to query. Consider materialized views for pre-aggregation (when available) or ensuring proper indexes on base tables."},
  {"num": 26, "question": "Which T-SQL clause should you use to include rows with no match in a JOIN?", "options": ["A. OUTER JOIN", "B. CROSS JOIN", "C. LEFT JOIN or RIGHT JOIN", "D. FULL OUTER JOIN"], "answer": "C", "explanation": "LEFT JOIN includes all rows from the left table regardless of matches, plus matched rows from the right table. RIGHT JOIN does the opposite. FULL OUTER JOIN includes all rows from both tables (matched or not). Use LEFT JOIN when you want to preserve all rows from the primary table."},
  {"num": 27, "question": "You need to create a composite primary key on a warehouse table. Which syntax is correct?", "options": ["A. PRIMARY KEY (col1, col2)", "B. CONSTRAINT pk_name PRIMARY KEY (col1, col2)", "c. UNIQUE CLUSTERED (col1, col2)", "d. Both A and B"], "answer": "D", "explanation": "Both syntaxes work: PRIMARY KEY (col1, col2) in CREATE TABLE or CONSTRAINT pk_name PRIMARY KEY (col1, col2) separately. The constraint name is optional in CREATE TABLE but recommended for clarity."},
  {"num": 28, "question": "What is the primary benefit of using a clustered index on a date column?", "options": ["A. Faster date calculations", "B. Range scan optimization", "C. Better date sorting", "D. Automatic partitioning"], "answer": "B", "explanation": "Clustered index on a date column naturally orders data by date, enabling efficient range scans for date-based queries. Queries with date ranges (WHERE date BETWEEN X AND Y) can quickly scan consecutive data pages rather than searching randomly."},
  {"num": 29, "question": "You need to create a filtered index that only includes active customers. Which syntax is correct?", "options": ["A. CREATE INDEX ... WHERE status = 'active'", "B. CREATE FILTERED INDEX ... ON active customers", "c. CREATE INDEX ... WITH FILTER (status = 'active')", "d. CREATE INDEX ... HAVING status = 'active'"], "answer": "A", "explanation": "CREATE INDEX ... WHERE status = 'active' creates a filtered index that includes only rows meeting the condition. This reduces index size and maintenance cost while supporting queries that only need active customers."},
  {"num": 30, "question": "Which T-SQL function should you use to find the first non-NULL value in a set?", "options": ["A. COALESCE", "B. ISNULL", "C. NULLIF", "D. FIRST_VALUE"], "answer": "A", "explanation": "COALESCE(value1, value2, ..., default) returns the first non-NULL value from the list. This is useful for providing default values when columns may contain NULL. ISNULL only checks for NULL and returns a replacement. NULLIF returns NULL if values match."},
  {"num": 31, "question": "You need to implement a many-to-many relationship between Orders and Products. How should you model this?", "options": ["A. Foreign keys in both tables", "B. Junction table with two foreign keys", "C. Bridge view", "D. Direct many-to-many relationship"], "answer": "B", "explanation": "Many-to-many relationships require a junction (bridge) table that stores pairs of foreign keys. OrderDetails table would have OrderID and ProductID foreign keys. This table can also include quantity, unit price, and other attributes of the relationship."},
  {"num": 32, "question": "Which T-SQL function should you use to calculate the median value?", "options": ["A. MEDIAN()", "B. PERCENTILE_CONT(0.5)", "C. AVG() with ORDER BY", "D. No built-in median function"], "answer": "B", "explanation": "T-SQL doesn't have a MEDIAN() function. Use PERCENTILE_CONT(0.5) within the ordered set to find the median. This returns the value at the 50th percentile, which is the median when data is ordered."},
  {"num": 33, "question": "You need to create a view for security that only shows specific columns. Is this recommended?", "options": ["A. Yes, for security", "B. No, use column-level permissions instead", "C. Yes, but views don't filter columns", "d. Use RLS instead"], "answer": "B", "explanation": "Views are not for security - use column-level permissions or row-level security instead. Views can be bypassed (SELECT * on base table). Use GRANT SELECT on specific columns or RLS predicates for actual data security."},
  {"num": 34, "question": "What is the purpose of the FILLFACTOR option in CREATE INDEX?", "options": ["A. How much free space to leave on pages", "b. How much to compress data", "c. How many pages to allocate", "d. How to handle page splits"], "answer": "A", "explanation": "FILLFACTOR determines how full pages should be when created or rebuilt. Higher fillfactor (e.g., 100%) creates full pages with less room for growth, but wastes space on updates. Lower fillfactor (e.g., 80%) leaves more room but requires more pages. Default is 100%."},
  {"num": 35, "question": "You need to create a computed column that calculates age from birthdate. Which expression is correct?", "options": ["A. DATEDIFF(year, birthdate, GETDATE())", "B. DATEDIFF(year, GETDATE(), birthdate)", "c. AGE(birthdate)", "d. TIMESTAMPDIFF(year, birthdate, GETDATE())"], "answer": "B", "explanation": "DATEDIFF(year, GETDATE(), birthdate) calculates age in years. DATEDIFF counts the boundary crossings of the specified datepart. Since GETDATE() is later than birthdate, this returns positive age. Handle leap years by adding a check for whether birthday hasn't occurred yet this year."},
  {"num": 36, "question": "Which T-SQL statement should you use to add a check constraint for positive values?", "options": ["A. CHECK (column > 0)", "B. ADD CONSTRAINT ck_name CHECK (column > 0)", "C. VALIDATE (column > 0)", "d. CONSTRAINT positive (column > 0)"], "answer": "B", "explanation": "ADD CONSTRAINT constraint_name CHECK (column > 0) adds a check constraint. The constraint name should be descriptive. Check constraints validate data during INSERT and UPDATE operations, rejecting rows that don't meet the condition."},
  {"num": 37, "question": "What is the difference between a clustered and nonclustered index?", "options": ["A. Clustered orders data, nonclustered doesn't", "B. Clustered is unique, nonclustered can be duplicated", "C. Only one clustered index per table, multiple nonclustered", "d. Clustered is primary key, nonclustered is secondary"], "answer": "C", "explanation": "A table can have only one clustered index (which determines physical order) but multiple nonclustered indexes. Clustered indexes contain the actual data; nonclustered indexes point to clustered index rows. Clustered is often on primary key by default."},
  {"num": 38, "question": "You need to write a query that returns the top 3 customers by sales, with ties allowed. Which pattern should you use?", "options": ["A. TOP 3 WITH TIES", "B. TOP 3", "c. ROW_NUMBER() OVER (ORDER BY sales)", "d. TOP 3 WITH TIES ON sales"], "answer": "A", "explanation": "TOP 3 WITH TIES returns all tied rows at the cutoff point. If the 3rd and 4th customers have the same sales, both are included (4 rows returned). Without WITH TIES, SQL would arbitrarily choose one of the ties."},
  {"num": 39, "question": "Which T-SQL feature allows you to temporarily disable a constraint?", "options": ["A. NOCHECK CONSTRAINT", "B. DISABLE CONSTRAINT", "c. SUSPEND CONSTRAINT", "d. IGNORE CONSTRAINT"], "answer": "A", "explanation": "NOCHECK CONSTRAINT disables a constraint temporarily for data loading or other operations. Use with ALTER TABLE. Re-enable with CHECK CONSTRAINT. Disabled constraints are not trusted for query optimization until re-enabled."},
  {"num": 40, "question": "You need to create a date dimension table for a warehouse. Which columns are essential?", "options": ["A. Date, Day, Month, Year only", "B. Date, Day, Month, Year, Quarter, Week, DayOfWeek", "c. Date key only", "d. Date and time only"], "answer": "B", "explanation": "A good date dimension should include Date key (integer), Date, Day, Month, Year, Quarter, Week, DayOfWeek, DayOfYear, etc. These columns support various time intelligence calculations in both SQL and DAX, making the dimension flexible for different analytical needs."},
  {"num": 41, "question": "What is the purpose of STATISTICS_UPDATE in a Fabric warehouse?", "options": ["A. Update query plans", "B. Update distribution statistics", "c. Update index statistics", "d. Update table metadata"], "answer": "B", "explanation": "STATISTICS_UPDATE updates distribution statistics about column values (histograms). The query optimizer uses these statistics to estimate row counts for JOIN conditions and WHERE clauses, improving query plan accuracy. Outdated statistics lead to poor query performance."},
  {"num": 42, "question": "You need to create a table that will have millions of rows inserted daily. Which distribution should you choose?", "options": ["A. ROUND_ROBIN", "B. HASH on date column", "c. REPLICATE dimension tables", "d. HEAP distribution"], "answer": "B", "explanation": "HASH distribution on a frequently filtered column (like date) provides good query performance for filters. Distributing by date spreads data across nodes for time-series queries. ROUND_ROBIN distributes randomly, which doesn't help query optimization."},
  {"num": 43, "question": "Which T-SQL function should you use to get the last day of the current month?", "options": ["A. EOMONTH(GETDATE())", "B. LAST_DAY(GETDATE())", "c. DATEADD(DAY, -1, DATEADD(MONTH, 1, GETDATE()))", "d. ENDOFMONTH(GETDATE())"], "answer": "A", "explanation": "EOMONTH(GETDATE()) returns the last day of the current month. This function handles leap years and different month lengths automatically. It's simpler than manually calculating the last day using DATEADD."},
  {"num": 44, "question": "What is the difference between a filtered index and a filtered statistics?", "options": ["A. Filtered index is for queries, filtered statistics is for updates", "B. Filtered index stores data, filtered statistics is metadata", "c. Filtered index is permanent, filtered statistics is temporary", "d. No difference"], "answer": "B", "explanation": "Filtered index is a physical index structure that stores index keys for rows meeting the filter. Filtered statistics are metadata about data distribution for filtered rows. Both optimize queries but serve different purposes - indexes for retrieval, statistics for planning."},
  {"num": 45, "question": "You need to implement a slowly changing dimension Type 6 in a warehouse. Which pattern should you use?", "options": ["A. Track all history in main table", "B. Use current flag and effective dates", "c. Store history in separate table", "d. Use temporal tables"], "answer": "C", "explanation": "SCD Type 6 tracks all historical changes in a separate history table. The main table contains current state, while the history table contains all versions with effective dates. This keeps the main table clean while preserving full audit trail of changes."},
  {"num": 46, "question": "Which T-SQL function should you use to parse a JSON string stored in a column?", "options": ["A. JSON_VALUE", "B. JSON_QUERY", "c. OPENJSON", "d. PARSE_JSON"], "answer": "A", "explanation": "JSON_VALUE extracts a scalar value from JSON at a specified path. For complex nested structures, use JSON_QUERY to extract JSON fragments or OPENJSON to parse into a table format. JSON_VALUE is simplest for extracting single values."},
  {"num": 47, "question": "What is the purpose of INCLUDE columns in a nonclustered index?", "options": ["A. Improve query performance by covering more columns", "B. Make the index clustered", "c. Enable compression", "d. Add more columns to the key"], "answer": "A", "explanation": "INCLUDE columns add non-key columns to the index leaf level, enabling index-only scans for queries that need those columns. This improves performance by avoiding table lookups. INCLUDE columns don't affect the index key order."},
  {"num": 48, "question": "You need to create a unique constraint on a column that already contains duplicate values. What must you do first?", "options": ["A", "Remove duplicates", "b. Create filtered index", "c. Use IGNORE_DUP_KEY", "d. Defer constraint checking"], "answer": "A", "explanation": "You must remove duplicates before creating a unique constraint. Use DELETE with ROW_NUMBER() or GROUP BY ... HAVING to remove duplicates. Alternatively, create a filtered unique index that only includes unique rows, excluding NULLs and duplicates."},
  {"num": 49, "question": "Which T-SQL function should you use to concatenate strings with a separator?", "options": ["A", "+ operator", "b. CONCAT_WS", "c. STRING_JOIN", "d. FORMAT function"], "answer": "C", "explanation": "CONCAT_WS(separator, string1, string2, ...) concatenates strings with a separator. Unlike CONCAT or +, CONCAT_WS automatically skips NULL values. The separator is only added between non-NULL values, making it ideal for building formatted strings."},
  {"num": 50, "question": "What is the primary benefit of using a clustered columnstore index (CCI) over a clustered rowstore index for analytical workloads?", "options": ["A", "Supports faster updates", "b. Better compression and scan performance", "c. Lower memory usage", "d. Enforces uniqueness"], "answer": "B", "explanation": "Clustered columnstore indexes provide superior compression (often 10x better) and faster scan performance for analytical workloads compared to rowstore. They compress data column-wise and optimize for bulk data operations. Rowstore is better for OLTP with many single-row updates."}
];
